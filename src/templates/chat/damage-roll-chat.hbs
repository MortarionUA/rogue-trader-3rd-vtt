<div class="rt-wrapper">
    {{#with rollData}}
    <div class="rt-chat">
        <h3>Damage Roll</h3>
        <div class="rt-roll">
            <h1>{{name}}</h1>
            <div class="rt-roll__divider"></div>

            <div class="rt-roll__row rt-roll__row--number">
                <div class="rt-roll__label">Formula</div>
                <div class="rt-roll__value">{{roll.formula}}</div>
            </div>
            <div class="rt-roll__row rt-roll__row--number">
                <div class="rt-roll__label">Damage Type</div>
                <div class="rt-roll__value">{{damageType}}</div>
            </div>

            <div class="rt-roll__notice">
                <span>Damage</span>
                <span>{{roll.total}}</span>
            </div>

            <div class="rt-roll__notice">
                <span>Penetration</span>
                <span>{{penetration}}</span>
            </div>

            {{#if targetActor}}
                <div class='rt-roll__controls-1'>
                    <div class="rt-control-button roll-control__assign-damage"
                            data-location="Body"
                            data-total-damage="{{roll.total}}"
                            data-total-penetration="{{penetration}}"
                            data-target-uuid="{{targetActor.uuid}}"
                            data-damage-type="{{damageType}}">
                        <span class="rt-control-button__span material-icons">api</span>
                        <span class="rt-control-button__label">Assign Damage</span>
                    </div>
                </div>
            {{/if}}
        </div>
    </div>
    {{/with}}
</div>
