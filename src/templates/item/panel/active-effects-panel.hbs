<div class="rt-panel rt-grid-col-6">
    <h1 class="rt-panel__header">Active Effects</h1>
    <div class="rt-table--border rt-activeeffectstable--border">
        <div class="table-row--head">
            <div class="table-cell--span2">Name</div>
            <div class="table-cell--last">
                {{#unless item.isEmbedded}}
                    <label class="rt-control-button effect-create">
                        <span class="rt-control-button__span material-icons">{{defaultVal icon "add_circle"}}</span>
                    </label>
                {{/unless}}
            </div>
        </div>
        {{#each effects as |effect|}}
            <div class="table-row">
                <div class="table-cell--settingstoggle">
                {{#unless @root.item.isEmbedded}}
                    <label class="rt-control-button effect-edit" data-effect-id="{{effect.id}}">
                        <span class="rt-control-button__span material-icons">settings</span>
                    </label>
                {{/unless}}
                </div>
                <div class="table-cell">
                    <span class="table-cell--description">{{effect.label}}</span>
                </div>
                <div class="table-cell">
                {{#unless @root.item.isEmbedded}}
                    {{#if disabled}}
                        <label class="rt-control-button effect-enable" data-effect-id="{{effect.id}}">
                            <span class="rt-control-button__span material-icons">flash_on</span>
                        </label>
                    {{else}}
                        <label class="rt-control-button effect-disable" data-effect-id="{{effect.id}}">
                            <span class="rt-control-button__span material-icons">flash_off</span>
                        </label>
                    {{/if}}
                    <label class="rt-control-button effect-delete" data-effect-id="{{effect.id}}">
                        <span class="rt-control-button__span material-icons">delete</span>
                    </label>
                {{/unless}}
                </div>
            </div>
        {{/each}}
    </div>
</div>
