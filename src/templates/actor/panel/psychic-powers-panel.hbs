<div class="rt-panel rt-grid-col-4 rt-grid-row-2">
    <h1 class="rt-panel__header">Psychic Powers</h1>
    <div class="rt-table--border rt-psychictable--border">
        <div class="table-row--head">
            <div class="table-cell--span2">Name</div>
            <div class="table-cell">Discipline</div>
            <div class="table-cell">Sustained</div>
            <div class="table-cell--last">
                <label class="rt-control-button item-create" data-type="psychicPower">
                    <span class="rt-control-button__span material-icons">{{defaultVal icon 'add_circle'}}</span>
                </label>
            </div>
        </div>
        {{#each actor.items as | item |}}
            {{#if item.isPsychicPower}}
                <div class="table-row item-drag" data-item-id="{{item.id}}" data-item-type="psychicPower">
                    <div class="table-cell--settingstoggle">
                        <label class="rt-control-button item-edit" data-item-id="{{item.id}}">
                            <span class="rt-control-button__span material-icons">{{defaultVal icon 'settings'}}</span>
                        </label>
                    </div>
                    <div class="table-cell--left">
                        {{> systems/rogue-trader-3rd/templates/actor/partial/trait-toggle.hbs toggle=(concat 'description_' item.id) text=item.name}}
                    </div>
                    <div class="table-cell">
                        <span class="display">{{item.system.discipline}}</span>
                    </div>
                    <div class="table-cell">
                        <span class="display">{{item.system.sustained}}</span>
                    </div>
                    <div class="table-cell--last">
                        <label class="rt-control-button item-roll" data-item-id="{{item.id}}">
                            <span class="rt-control-button__span material-icons">{{defaultVal icon 'casino'}}</span>
                        </label>
                        {{#if (isPsychicAttack item)}}
                            <label class="rt-control-button item-damage" data-item-id="{{item.id}}">
                                <span class="rt-control-button__span material-icons">{{defaultVal icon 'bolt'}}</span>
                            </label>
                        {{else}}
                            <label class="rt-control-button item-vocalize" data-item-id="{{item.id}}">
                                <span class="rt-control-button__span material-icons">{{defaultVal icon 'record_voice_over'}}</span>
                            </label>
                        {{/if}}
                        <label class="rt-control-button item-delete" data-item-id="{{item.id}}">
                            <span class="rt-control-button__span material-icons">{{defaultVal icon 'delete'}}</span>
                        </label>
                    </div>
                    <div class="table-cell--description description_{{item.id}}" {{hideIfNot
                        (isExpanded (concat 'description_' item.id ))}}>
                        <span class="display">{{item.system.description}}</span>
                    </div>
                </div>
            {{/if}}
        {{/each}}
    </div>
</div>
