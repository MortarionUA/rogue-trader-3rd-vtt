<form class="rt-wrapper">
    <div class="rt-panel spacer">
        <h1 class="rt-panel__header">Release Notes: {{version}}</h1>
        <ul>
            {{#each notes as |note|}}
                <li>{{note}}</li>
            {{/each}}
        </ul>
    </div>
</form>
